export interface User {
  labelInputName: string;
  labelEmail: string;
  labelInputPassword: string;
}
export interface UserStorage {
  id: string;
  name: string;
  password: string;
  email: string;
}
