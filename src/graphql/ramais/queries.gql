query GetRamaisForPage($page: ID!) {
  getRamaisForPage(page: $page) {
    id
    name
    sector
    number
  }
}
query GetLenghtRamais($maxPages: String!) {
  getLenghtRamais(maxPages: $maxPages)
}
